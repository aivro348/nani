{
  "name": "sathishbro-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build --workspaces --if-present",
    "start": "concurrently \"npm run start -w frontend\" \"npm run start -w backend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
